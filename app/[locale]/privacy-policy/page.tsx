import {notFound} from "next/navigation";import{getDictionary,Locale,locales}from"../../i18n";export default async function Privacy({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!locales.includes(locale as Locale))notFound();const d=getDictionary(locale);return <article className="shell legal"><h1>{d.legal.privacy}</h1><p>{d.legal.updated}</p>{d.legal.privacyBody.map((x,i)=><section key={x}><h2>{locale==="es"?["Información que recopilamos","Cómo usamos la información","Contacto"][i]:["Information we collect","How we use information","Contact"][i]}</h2><p>{x}</p></section>)}</article>}
