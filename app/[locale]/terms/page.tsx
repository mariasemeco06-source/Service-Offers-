import type{Metadata}from"next";import {notFound} from "next/navigation";import{getDictionary,Locale,locales}from"../../i18n";export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;return{title:locale==="es"?"Términos del Sitio":"Website Terms",alternates:{canonical:`/${locale}/terms`,languages:{"en-US":"/en/terms","es-US":"/es/terms"}}}}export default async function Terms({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!locales.includes(locale as Locale))notFound();const d=getDictionary(locale);return <article className="shell legal"><h1>{d.legal.terms}</h1><p>{d.legal.updated}</p>{d.legal.termsBody.map((x,i)=><section key={x}><h2>{locale==="es"?["Propósito informativo","No se crea cobertura","Uso aceptable"][i]:["Informational purpose","No coverage created","Acceptable use"][i]}</h2><p>{x}</p></section>)}</article>}
