import {notFound} from "next/navigation";import{getDictionary,Locale,locales}from"../../i18n";export default async function Terms({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!locales.includes(locale as Locale))notFound();const d=getDictionary(locale);return <article className="shell legal"><h1>{d.legal.terms}</h1><p>{d.legal.updated}</p>{d.legal.termsBody.map((x,i)=><section key={x}><h2>{locale==="es"?["Propósito informativo","No se crea cobertura","Uso aceptable"][i]:["Informational purpose","No coverage created","Acceptable use"][i]}</h2><p>{x}</p></section>)}</article>}
