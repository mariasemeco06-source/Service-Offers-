import type{Metadata}from"next";import Link from "next/link";import{notFound}from"next/navigation";import{getDictionary,Locale,locales}from"../../i18n";export const metadata:Metadata={title:"Thank You",robots:{index:false,follow:false}};export default async function Thanks({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!locales.includes(locale as Locale))notFound();const d=getDictionary(locale);return <section className="thank"><div><span>✓</span><h1>{d.thanks.title}</h1><p>{d.thanks.text}</p><Link className="button primary" href={`/${locale}`}>{d.thanks.button}</Link></div></section>}
